# a host definition for the gateway of the default route
define host {
        host_name   gateway
        alias       Default Gateway
        address     GW
        use         generic-host
        }
